#pragma once

/**
 * Returns the index of the first occurrence of any character in s2 in the
 * string s1, or -1 if no characters in s2 are found in s1.
*/
int any(const char s1[], const char s2[]);

int findFirst(const char s[], const int c);
